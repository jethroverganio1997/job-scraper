

# API Keys (Optional - for Crawl4AI AI features)
OPENAI_API_KEY=""



# LinkedIn Job Scraper Configuration
# You can customize these values without modifying the code

# Job search keywords (default: software engineer)
DEFAULT_KEYWORDS=software engineer

# Job location (default: Taiwan)
# Examples: Taiwan, United States, United Kingdom, Canada, Australia, Singapore
DEFAULT_LOCATION=Philippines

# Maximum number of jobs to scrape (default: 5)
DEFAULT_MAX_JOBS=5

# Maximum number of pages to search (default: 2)
DEFAULT_MAX_PAGES=2

# Time filter for job search (default: 86400 seconds = 24 hours)
# Other options: 604800 (7 days), 2592000 (30 days), 0 (all time)
DEFAULT_TIME_FILTER=43200

# Seek Job Scraper Configuration
# You can customize these values without modifying the code

# Seek-specific keywords (overrides default if set)
SEEK_KEYWORDS=software engineer

# Seek-specific location (overrides default if set)
SEEK_LOCATION=Philippines

# Maximum number of Seek jobs to scrape
SEEK_MAX_JOBS=5

# Maximum number of Seek pages to search
SEEK_MAX_PAGES=3

# Date filter for Seek job search
# Options: 1=today, 3=3days, 7=1week, 30=1month
SEEK_DATE_FILTER=1